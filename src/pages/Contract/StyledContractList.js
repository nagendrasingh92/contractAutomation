
import { styled } from '@mui/material/styles';

const StyledContractList = styled('div')(
  ({theme}) => `
    width: 60%;
    margin: auto;
    .nameWrap{
        
    }
`);

export default StyledContractList